# Copy this file to `.env` and fill in your own values.
# The agent auto-loads `.env` from the agent/ directory on startup.
AUTOMATION_USER=emuser
AUTOMATION_PASS=empass

# Optional: Hugging Face token for faster model downloads (https://huggingface.co/settings/tokens)
# HF_TOKEN
